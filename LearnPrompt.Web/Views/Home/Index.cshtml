@{
    ViewData["Title"] = "Home";
}

@if (!(User?.Identity?.IsAuthenticated ?? false))
{
    <section class="mx-auto max-w-4xl rounded-3xl bg-white p-10 shadow-xl shadow-indigo-100">
        <p class="inline-flex items-center gap-2 rounded-full bg-indigo-50 px-3 py-1 text-sm font-medium text-indigo-600">AI destekli kurs platformu</p>
        <h1 class="mt-6 text-4xl font-bold tracking-tight text-slate-900 sm:text-5xl">Kendi prompt derslerinizi dakikalar içinde oluşturun</h1>
        <p class="mt-4 text-lg text-slate-600">Belge yükleyin, kursunuzu tasarlayın ve üyelerinizle paylaşın. Başlamak için lütfen giriş yapın.</p>
        <div class="mt-8 flex flex-wrap gap-4 text-sm font-semibold">
            <a class="rounded-full bg-indigo-600 px-8 py-3 text-white shadow-lg shadow-indigo-200 transition hover:bg-indigo-500"
               asp-area="Identity"
               asp-page="/Account/Login">Giriş Yap</a>
            <a class="rounded-full border border-slate-200 px-8 py-3 text-slate-700 transition hover:border-indigo-200"
               asp-area="Identity"
               asp-page="/Account/Register">Hesap Oluştur</a>
        </div>
    </section>
}
else
{
    <section class="grid gap-6 lg:grid-cols-2">
        <div class="rounded-3xl border border-slate-200 bg-white p-8 shadow-sm">
            <p class="text-sm font-semibold text-indigo-600">Hoş geldin @User.Identity.Name</p>
            <h1 class="mt-4 text-3xl font-semibold text-slate-900">Öğrenmeye hazır mısın?</h1>
            <p class="mt-3 text-slate-600">Yeni kurs oluştur, mevcut kurslarını düzenle ve dosyalarını yükle ve AI destekli öğrenme sürecin için en iyi prompt'u oluştur.</p>
            <div class="mt-6 flex flex-wrap gap-3 text-sm font-semibold">
                <a class="rounded-lg bg-indigo-600 px-5 py-3 text-white shadow-sm transition hover:bg-indigo-500"
                   asp-controller="Courses"
                   asp-action="Create">Yeni Kurs Oluştur</a>
                <a class="rounded-lg border border-slate-200 px-5 py-3 text-slate-700 transition hover:border-indigo-200"
                   asp-controller="Courses"
                   asp-action="Index">Kurslarım</a>
            </div>
        </div>
        <div class="rounded-3xl border border-slate-200 bg-white p-8 shadow-sm">
            <h2 class="text-xl font-semibold text-slate-900">Son İpuçları</h2>
            <ul class="mt-4 space-y-3 text-sm text-slate-600">
                <li class="rounded-2xl border border-slate-100 bg-slate-50/80 p-4">
                    Dersine ait PDF, doküman veya notları yükle. LearnPrompt bu içerikleri senin adına analiz eder ve anlamlandırır.
                </li>
                <li class="rounded-2xl border border-slate-100 bg-slate-50/80 p-4">
                    Yüklediğin içeriklerden, dersine özel çalışan bir <strong>akıllı yapay zekâ yönergesi</strong> otomatik olarak oluşturulur.
                </li>
                <li class="rounded-2xl border border-slate-100 bg-slate-50/80 p-4">
                    Bu yönergeyi kullanarak GPT, Gemini veya DeepSeek gibi yapay zekâlarla dersini sorular sorarak, özet çıkararak veya konu çalışarak yönetebilirsin.
                </li>
            </ul>
        </div>
    </section>
}
